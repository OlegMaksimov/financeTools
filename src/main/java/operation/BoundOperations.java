package operation;

/**
 * Операции над облигациями
 */
public class BoundOperations {

//    public static Double getCostBound(Bound bound, Double r){
//        Double discont = 0d;
//
//        for (int i = 1; i < bound.getPeriodcCount()+1; i++) {
//
//            discont += (bound.getCouponCost()/(Math.pow((1+r),i)));
//        }
//        discont += (bound.getPrice()/((Math.pow(1+r,bound.getPeriodcCount()))));
//        return discont;
//    }
}
